{
  "transport_mode": {
    "type": "string",
    "description": "通道模式：bridge = 走 arena-bridge 容器（默认，和以前完全一样）；direct = 插件直连 arena-browser，不需要 bridge 容器",
    "options": ["bridge", "direct"],
    "default": "bridge"
  },
  "bridge_url": {
    "type": "string",
    "description": "LMArenaBridge 根地址或 /api/v1 地址（仅 bridge 模式使用）",
    "default": "http://arena-bridge:8000"
  },
  "bridge_api_key": {
    "type": "string",
    "description": "LMArenaBridge API Key（默认零配置，留空即可）",
    "secret": true,
    "default": ""
  },
  "openai_proxy_enabled": {
    "type": "bool",
    "description": "开启 OpenAI 兼容图像中转接口（/v1/models、/v1/images/generations、/v1/images/edits）",
    "default": false
  },
  "openai_proxy_host": {
    "type": "string",
    "description": "OpenAI 中转监听地址；默认 127.0.0.1 仅本机可访问，需要给其它机器使用时填 0.0.0.0 并配置 API Key",
    "default": "127.0.0.1"
  },
  "openai_proxy_port": {
    "type": "int",
    "description": "OpenAI 中转监听端口",
    "default": 18081
  },
  "openai_proxy_api_key": {
    "type": "string",
    "description": "OpenAI 中转 API Key；监听非本机地址时必填，客户端使用 Authorization: Bearer 该值",
    "secret": true,
    "default": ""
  },
  "browser_cdp_url": {
    "type": "string",
    "description": "arena-browser 的 CDP 地址（仅 direct 模式）。一般不用改；只有 AstrBot 和浏览器不在同一个 Docker 网络、且自动改试 host.docker.internal / 172.17.0.1 / 127.0.0.1 都连不上时，才填 http://服务器IP:9223",
    "default": "http://arena-browser:9223"
  },
  "browser_gateway_url": {
    "type": "string",
    "description": "验证链接网关地址（可选，正常部署留空）。留空时插件会自动读浏览器容器里 setup.sh 写好的 gateway-url.txt；只有老部署或自建部署读不到时，才填 http://服务器IP:6081（端口用你映射到浏览器容器 6081 的那个宿主机端口，改过就填改过的，例如 http://服务器IP:7081）",
    "default": ""
  },
  "browser_vnc_url": {
    "type": "string",
    "description": "noVNC 直连地址（可选）；填了它就能自动推出网关地址（按默认 6081 端口推算，改过端口请直接填上面那一栏），也在拿不到签名密钥时作为兜底链接",
    "default": ""
  },
  "interactive_link_secret": {
    "type": "string",
    "description": "验证链接签名密钥（留空即可）：留空时插件会自动从服务器浏览器的 /run/secrets/interactive_link_secret 读取，和网关永远一致；只有非标准部署才需要手填",
    "secret": true,
    "default": ""
  },
  "interactive_link_ttl": {
    "type": "int",
    "description": "验证链接有效期（秒，仅 direct 模式使用）",
    "default": 900
  },
  "allow_stealth_models": {
    "type": "bool",
    "description": "放行灰测（隐身）模型，蒙娜丽莎在这一类里（仅 direct 模式使用；bridge 模式由 .env 控制）",
    "default": true
  },
  "default_model": {
    "type": "string",
    "description": "默认画图模型；留空使用 Bridge 返回的第一个模型",
    "default": "gpt-image-2 (medium)"
  },
  "request_timeout": {
    "type": "int",
    "description": "模型请求和图片下载超时（秒）",
    "default": 300
  },
  "model_cache_seconds": {
    "type": "int",
    "description": "模型列表缓存时间（秒）",
    "default": 30
  },
  "model_list_limit": {
    "type": "int",
    "description": "每个模型列表最多展示数量（画图/灰测各自计算）",
    "default": 50
  },
  "model_health_cache_seconds": {
    "type": "int",
    "description": "模型状态码缓存时间（秒）",
    "default": 30
  },
  "image_models_only": {
    "type": "bool",
    "description": "模型列表只显示可画图模型（关闭后显示全部 312 个）",
    "default": true
  },
  "max_image_bytes": {
    "type": "int",
    "description": "输入参考图最大字节数（图生图上传用）",
    "default": 10485760
  },
  "max_output_image_bytes": {
    "type": "int",
    "description": "允许下载的生成图片最大字节数（超过则报错）",
    "default": 67108864
  },
  "send_image_max_bytes": {
    "type": "int",
    "description": "发送前的图片大小阈值，超过会自动压缩/降采样（需要 Pillow）",
    "default": 8388608
  },
  "max_input_images": {
    "type": "int",
    "description": "一次图生图最多发送的参考图数量",
    "default": 4
  },
  "max_output_images": {
    "type": "int",
    "description": "一次请求最多发送的生成图片数量",
    "default": 1
  },
  "max_saved_outputs": {
    "type": "int",
    "description": "插件本地最多保留的生成图片数量",
    "default": 32
  },
  "max_queue_depth": {
    "type": "int",
    "description": "同时排队等待出图的最大任务数，超出直接拒绝",
    "default": 5
  },
  "rate_limit_retries": {
    "type": "int",
    "description": "上游限流（429）后的自动重试次数",
    "default": 2
  },
  "rate_limit_max_wait": {
    "type": "int",
    "description": "限流重试的单次最长等待秒数（优先遵循 Retry-After）",
    "default": 30
  },
  "preset_prompts": {
    "type": "list",
    "description": "预设提示词，每行一条：名字=提示词。提示词里的 {} 是补充描述插入位置，不写就接在后面。聊天里 /竞技场预设添加 保存的同名预设优先。",
    "default": [
      "赛博=cyberpunk city at night, neon reflections, rain-slick streets, cinematic lighting, ultra detailed, {}",
      "手绘=hand-drawn watercolor illustration, soft edges, warm paper texture, delicate linework, {}",
      "写实=photorealistic, 85mm portrait lens, shallow depth of field, natural window light, skin texture detail, {}",
      "动漫=anime key visual, clean cel shading, vibrant colors, expressive eyes, detailed background, {}",
      "手办=1/7 scale figure, PVC statue on round base, studio product photo, glossy finish, {}"
    ]
  }
}
